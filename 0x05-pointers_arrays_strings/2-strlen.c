#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - Prints the length of a string
 * @s: String
 *
 * Return: 0 (Success)
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
